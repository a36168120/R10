import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  speakerContainer: {
    flex: 1,
  },

  container: {
    flex: 1,
  },

  image: {
    height: 50,
    width: 50,
    resizeMode: 'contain',
  },
});

export default styles;
